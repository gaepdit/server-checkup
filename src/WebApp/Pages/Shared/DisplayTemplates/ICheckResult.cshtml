@model CheckServerSetup.Checks.ICheckResult?

@if (Model == null)
{
    return;
}

@switch (Model.ResultContext)
{
    case CheckServerSetup.Checks.Context.Success:
        <h2 class="text-success">Success</h2>
        break;
    case CheckServerSetup.Checks.Context.Error:
        <h2 class="text-danger">Errors occurred</h2>
        break;
    case CheckServerSetup.Checks.Context.Warning:
        <h2 class="text-warning">Issues occurred</h2>
        break;
    case CheckServerSetup.Checks.Context.Info:
    default:
        break;
}

@if (Model.Messages.Count > 0)
{
    <ul class="list-group">
        @foreach (var message in Model.Messages)
        {
            @Html.DisplayFor(m => message)
        }
    </ul>
}
